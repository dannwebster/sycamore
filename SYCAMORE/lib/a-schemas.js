/*Events = new Mongo.Collection("Events");
Events.attachSchema(new SimpleSchema({
    title: {
        type: String,
        label: "Title",
        max: 200
    },
    eventType: {
        type: String,
        label: "Event Type",
        allowedValues: ["Field Trip", "In-School Event","After School Event", "Parent Teacher Conference", "Other", "School Session","No School Day"],
        autoform: {
            afFieldInput: {
                firstOption: "(Select an Event Type)"
            }
        }
    },
    startDate: {
        type: String,
        label: "Start Date",
        max: 200
    },
    endDate: {
        type: String,
        label: "End Date",
        max: 200
    },
    description: {
        type: String,
        label: "Details",
        max: 10000
    },
}));

AFPikadaySchema =new SimpleSchema({
    startDate: {
        type: String,
        optional: false
    },
    endDate: {
        type: String,
        optional: true
    }
});*/
